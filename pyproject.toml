[project]
name = "robo-chefe-discord"
version = "2.0.0"
description = "Bot de administração, atendimento e utilidades para servidores Discord."
requires-python = ">=3.12"
dependencies = [
    "discord-py>=2.6.0",
    "python-dotenv>=1.0.0",
    "aiosqlite>=0.19.0",
]
